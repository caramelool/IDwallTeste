<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentTransitions">true</item>

        <item name="android:buttonStyle">@style/AppTheme.ButtonStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.FeedList" parent="@style/AppTheme.NoActionBar">
        <item name="android:windowExitTransition">@transition/feed_list_exit</item>
        <item name="android:windowReenterTransition">@transition/feed_list_reenter</item>
    </style>

    <!--<style name="AppTheme.FeedDetail" parent="Theme.AppCompat.Light.Dialog">-->
        <!--<item name="windowNoTitle">true</item>  -->
    <style name="AppTheme.FeedDetail" parent="@style/AppTheme.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowEnterTransition">@transition/feed_detail_enter</item>
    </style>

    <style name="AppTheme.ButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="colorButtonNormal">@android:color/black</item>
    </style>

</resources>
